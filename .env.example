# ===============================================================
# ==                Environment Variables                      ==
# ===============================================================
#
# Copy this file to .env and fill in the appropriate values.
# This file serves as a template for the environment variables
# needed to run the application.

# --- Application Configuration ---
# Port for the backend application
APP_PORT=3000

# Set to 'development' or 'production'
NODE_ENV=development

# --- Database Configuration (for Node.js App) ---
# Full database connection URL for Prisma and the Node.js app.
# IMPORTANT:
# - For Docker: Use 'db' as the host, as it's the service name in docker-compose.yml.
# - For Local Dev: Change 'db' to 'localhost'.
DATABASE_URL="postgresql://tec7user:tec7password@db:5432/tecnica7_prod_db?schema=public"

# --- PostgreSQL Service Configuration (for docker-compose) ---

# --- Admin seed credentials (optional) ---
# Used by prisma/seed.ts. If not set, defaults to admin@example.com / admin123
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin123
ADMIN_FIRST_NAME=Admin
ADMIN_LAST_NAME=User
# These variables are ONLY used by the 'db' service in docker-compose.yml
# to initialize the PostgreSQL container for the first time.
# The user, password and db name should match the ones in DATABASE_URL above.
DB_USER=tec7user
DB_PASSWORD=tec7password
DB_NAME=tecnica7_prod_db

# Port on the host machine to map to the PostgreSQL container's port 5432.
# Change this if you already have a service running on port 5432.
DB_PORT=5432

# --- Mail Service Configuration (for Nodemailer) ---
# Example using Ethereal for testing. Replace with your actual SMTP provider.
MAIL_HOST="smtp.ethereal.email"
MAIL_PORT=587
MAIL_SECURE=false
MAIL_USER="your_ethereal_user@ethereal.email"
MAIL_PASS="your_ethereal_password"
MAIL_FROM="noreply@tecnica7.com"
MAIL_TO="admin@tecnica7.com"

# --- Security & API Keys ---
# A strong, secret key for signing JSON Web Tokens.
# Generate one using: openssl rand -base64 32
JWT_SECRET="your_super_secret_jwt_key"

# Your API key for Google Gemini
GEMINI_API_KEY="your_gemini_api_key_here"
